local ZoneConfig = {}

-- Safe Zone (Wandering Caravan)
ZoneConfig.SAFE_ZONE_RADIUS = 100
ZoneConfig.SAFE_ZONE_HEIGHT = 15
ZoneConfig.SAFE_ZONE_SPEED = 5

-- Emote Zone (King's Court)
ZoneConfig.EMOTE_ZONE_SIZE = Vector3.new(30, 1, 30)
ZoneConfig.EMOTE_ZONE_POS = Vector3.new(0, 0, 0)

return ZoneConfig

